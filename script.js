const button = document.getElementById("startBtn");
const statusText = document.getElementById("status");

button.addEventListener("click", () => {
  statusText.textContent = "🚀 Journey Started! Building skills every day.";
});
